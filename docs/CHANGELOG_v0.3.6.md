# CHANGELOG — v0.3.6 Delta
- Added `src/data/traits-matrix.schema.json` (authoritative schema for AI traits/behaviors).
- Added `src/data/newsletter.templates.json` (seasonal & rivalry strings; safe to ignore at runtime).
- Added `src/data/ftue.config.json` (prompts + toggles; engine can read or skip).
- Added `delta/v0.3.6.js` (no‑op runtime banner; only active if referenced).
- Added `tools/apply-delta.command` and `tools/verify-preflight.command` to streamline macOS flows.
